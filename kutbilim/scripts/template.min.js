const ANIMATION_SPEED=100;$(function(){$("#menu").superfish({speed:"fast",cssArrows:false})});$(function(){let gallery=$("#gallery");if(gallery.length){gallery.sliderPro({width:"100%",aspectRatio:1.77777,imageScaleMode:"contain",loop:false,slideDistance:15,fade:true,autoplay:false,buttons:false,keyboard:false,thumbnailWidth:75,thumbnailHeight:50})}});$(function(){let shave=$("[data-shave]");if(shave.length){shave.each(function(){shave.shave($(this).data("shave"))})}});$(function(){let contacts=$("#contacts");if(contacts.length){let map=contacts.find(".map"),coords=map.data("coords").split(",");ymaps.ready(init);function init(){map=new ymaps.Map(map[0],{controls:["fullscreenControl","zoomControl"],center:coords,zoom:17},{suppressMapOpenBlock:true});map.behaviors.disable(["drag","scrollZoom"]);map.container.events.add("fullscreenenter",function(){map.behaviors.enable(["drag","scrollZoom"])});map.container.events.add("fullscreenexit",function(){map.behaviors.disable(["drag","scrollZoom"])});map.geoObjects.add(new ymaps.Placemark(coords))}}});